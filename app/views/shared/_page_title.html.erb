<%
  # デフォルト値の設定
  tag_name = local_assigns.fetch(:tag, "h1")
  css_class = local_assigns.fetch(:css_class, "text-3xl font-bold mb-6")
  spec_class = local_assigns.fetch(:spec_class, "")
  full_class = "#{css_class} #{spec_class}".strip
%>

<%= content_tag tag_name, title, class: full_class %>