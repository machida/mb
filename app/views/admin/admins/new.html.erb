<%= render PageTitleComponent.new(title: "新規管理者追加") %>

<div class="a--page-body py-6">
  <div class="l--container is-sm">
    <%= form_with model: [:admin, @admin], local: true, class: "space-y-6" do |f| %>
      <%= render FormErrorsComponent.new(object: @admin) %>

      <div>
        <header class="a--card-header">
          <h2 class="a--heading is-md spec--admin-form-title">管理者情報</h2>
        </header>
        <div class="a--card">
          <div class="space-y-4">
            <div class="a--form-item">
              <%= f.label :user_id, "ユーザーID", class: "a--form-label mb-1" %>
              <%= f.text_field :user_id, required: true, class: "a--text-input spec--admin-user-id-input" %>
              <p class="a--form-help mt-1">記事の著者として表示されるIDです（英数字のみ）</p>
            </div>

            <div class="a--form-item">
              <%= f.label :email, "メールアドレス", class: "a--form-label mb-1" %>
              <%= f.email_field :email, required: true, class: "a--text-input spec--admin-email-input" %>
              <p class="a--form-help mt-1">ログイン時に使用するメールアドレスです</p>
            </div>

            <div class="a--form-item">
              <%= f.label :password, "初期パスワード", class: "a--form-label mb-1" %>
              <%= f.password_field :password, required: true, class: "a--text-input spec--admin-password-input" %>
              <p class="a--form-help mt-1">8文字以上で設定してください</p>
            </div>

            <div class="a--form-item">
              <%= f.label :password_confirmation, "パスワード確認", class: "a--form-label mb-1" %>
              <%= f.password_field :password_confirmation, required: true, class: "a--text-input spec--admin-password-confirmation-input" %>
              <p class="a--form-help mt-1">確認のため同じパスワードを入力してください</p>
            </div>
          </div>
        </div>
      </div>

      <div class="a--card is-info">
        <div class="flex items-start">
          <svg class="w-5 h-5 text-blue-600 mt-0.5 mr-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"></path>
          </svg>
          <div>
            <h3 class="text-sm font-medium text-blue-800 mb-1">初回ログイン後の対応について</h3>
            <p class="text-sm text-blue-700">
              追加された管理者は初回ログイン後、管理画面にパスワード変更を促すメッセージが表示されます。
              セキュリティのため、必ず初期パスワードから変更するよう案内してください。
            </p>
          </div>
        </div>
      </div>

      <div class="flex gap-4">
        <%= f.submit "管理者を追加", class: "a--button is-lg is-primary spec--create-admin-button" %>
        <%= link_to "キャンセル", admin_admins_path, class: "a--button is-lg is-border-secondary spec--cancel-button" %>
      </div>
    <% end %>
  </div>
</div>
